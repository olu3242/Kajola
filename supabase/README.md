# Supabase

This directory holds Supabase-related migrations, edge functions, and configuration.

- `migrations/` — SQL migration files for the Postgres schema
- `functions/` — Supabase Edge Functions for API and webhooks
